sample_dag:
  dag_id: sample_dag
  params: {}
  default_args:
    owner: airflow
    start_date: '2026-01-01'
    retries: 1
    retry_delay: 300.0
  schedule: 0 0 * * ? *
  tasks:
    task1:
      operator: airflow.providers.standard.operators.python.PythonOperator
      op_args: [42]
      op_kwargs: {}
      outlets: []
      params: {}
      priority_weight: 1
      python_callable: simple_python_context_manager.my_function
      retries: 1
      retry_delay: 300.0
      retry_exponential_backoff: false
      task_id: task1
      trigger_rule: all_success
      wait_for_downstream: false
      dependencies: []
